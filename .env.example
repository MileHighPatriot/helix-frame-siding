# Optional. When set, estimate, permit, and contact requests are POSTed here
# after a reference number is issued. The site works with this left blank.
LEAD_WEBHOOK_URL=

# Optional address included in the webhook payload so a mailer can route it.
LEAD_NOTIFY_EMAIL=
